npm install -g @vscode/vsce
vsce package

bugs
  deleteicon broken
  getlabel series.vue no symbols
     text: getLabel, No topSymbols found series.vue

todo 
  sort folders by name or recently used
  commands to go to prev/next item clicks
    add shift key to prev/next keys
  cmd to delete all marks in file
  add plugin support, eolIsSafe for now
  auto scroll
    highlight function
    scroll to top for functions
    min top and bottom border lines
    don't scroll if mark visible in window
  hide all marks, restore on any edit
  add names to marks
    edit menu
    don't delete named marks so names preserved
    delete named tokens when X menu used
  settings
  track gen1 marks with edits
  check competitor

impossible?
  invisible tokens
  toggle collapsed/expanded on file item click

not needed?
  undo
  automatic suggestions based on visits
    count visits in each symbol block
    show in sidebar with question mark
    click on sidebar to accept and create bookmark
    limit suggestions (setting)
  
settings
  sort folders by name or recently used
  show bookmark word in token
  fall back to gen1 
  max suggestion count
  smart scroll 
    scroll to top for functions
    don't scroll if mark visible in window
    dumb scroll top/middle/bottom/smart
    min num lines above and below selection
  max num of enclosing folder names
  clean trailing empty comments and whitespace
  open sidebar when adding mark
  files to ignore
  language types to ignore
  ignore .gitignore
  show in gutter
    gutter color
  show in scrollbar
    scrollbar color
  highlight line options
    none
    blick fast
    blink slow
    fade fast
    fade slow
    persist
  highlight function same options

testing
  find missing awaits from util redirects
  test dark theme "window color"
  moving a project folder
  mark after an unclosed string

features
  sticky
  prev/next sidebar item
  smart sidebar
    pointers
    line numbers
    breadcrumbs
    compressed code
  smart scrolling
  small ...
    click on token to delete
